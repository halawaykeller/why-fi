import React, { Component } from 'react'
import PropTypes from 'prop-types';

import styled from 'styled-components';
import { content } from '../data/graph-content'

const HookContent = (props) => (
    
    <p>{content}</p>
);

export default HookContent;
    